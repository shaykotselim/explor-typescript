
function add(first: number, second:number): number {
    const result = first + second;
    return result;

}
const add2 = (num1: number, num2: number): number => num1 + num2;

const doMath = (x:number, y:number): void =>{
    
}
